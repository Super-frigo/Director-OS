[project]
name = "director-os"
version = "0.1.0"
description = "AI-native filmmaking operating system"
requires-python = ">=3.11"
dependencies = [
    "jieba>=0.42",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
mcp = ["mcp[cli]"]

[project.scripts]
director-os = "director_os.cli:main"
